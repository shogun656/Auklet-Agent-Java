{
  "type": "object",
  "required": [
    "brokers",
    "port"
  ],
  "properties": {
    "brokers": {
      "type": "string",
      "pattern": "^(.+)$"
    },
    "port": {
      "type": "string",
      "pattern": "^(.+)$"
    }
  }
}
